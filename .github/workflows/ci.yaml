name: ContinuosIntegration_02

on:
    pull_request:
        branches:
            - master
            - develop
            - feature*
jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        if: startsWith( github.event.pull_request.head.ref , 'feature/' )

        steps:
            - name: checkout code
              uses: actions/checkout@v3
          
            - name: Setup Go
              uses: actions/setup-go@v2
              with:
                go-version: 1.20
            
            - name: Build
              run: go build -o app